    /**
     * This “bar” method takes a “Baz” argument and does bar stuff
     */
    bar: function(baz) {
        doBarStuffWith(baz);
    }



    /**
     * Does bar stuff
     * @param {Baz} baz this argument is used to do bar stuff
     */
    bar: function(baz) {
        doBarStuffWith(baz);
    }



/**
  * This model represents a book in our application.
  * @class
  */
var Book = Backbone.Model({ ...



/**
  * This model represents a book in our application.
  * @param {object} attributes
  * @param {string} attributes.title the book's title
  * @param {string} attributes.description the description of the book
  * @param {integer} attributes.pageLength the number of pages
  */
Book = Backbone.Model({ ...